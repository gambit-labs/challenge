export function register(user) {
  console.log(user)
  // Send to server
  // return http.post(apiEndpoint, {
  //   email: user.username,
  //   password: user.password,
  //   name: user.name
  // });
}
